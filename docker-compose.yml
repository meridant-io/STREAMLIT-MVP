services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./auth_config.yaml:/app/auth_config.yaml:ro
    restart: unless-stopped

{
    "name": "My App",
    "postCreateCommand": "sqlite3 data/meridant.db < seed.sql && sqlite3 data/meridant_frameworks.db < seed_frameworks.sql && echo \"$AUTH_CONFIG\" > auth_config.yaml",
    "forwardPorts": [
        8501
    ],
    "postStartCommand": "docker compose up --build"
}
